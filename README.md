const KEY="arefeh_growth_v2";
const today=()=>new Date().toISOString().slice(0,10);
const seed={profile:{name:"عارفه",grade:"دهم",goal:"پزشکی → جراحی پلاستیک",alt:"دندانپزشکی",motivation:"پول، آینده شغلی و موفقیت"},
targets:{study:5,language:20,book:20,phone:60,weekly:60},
tasks:[["ریاضی","درس",2],["زیست","درس",2],["شیمی/فیزیک","درس",2],["تست و تحلیل","کنکور",2],["زبان ۲۰ دقیقه","زبان",1],["کتاب ۲۰ دقیقه","رشد",1],["ورزش طبق برنامه","ورزش",1],["رعایت سقف گوشی","عادت",2],["خواب طبق برنامه","عادت",1],["بیداری طبق برنامه","عادت",1]].map((x,i)=>({id:i,title:x[0],cat:x[1],points:x[2],done:false})),
logs:[],exams:[],books:[],sports:[],goals:[
["پزشکی","قبولی در یکی از بهترین دانشگاه‌های علوم پزشکی کشور",0],["نظم","ساختن عادت انجام برنامه",0],["پشتکار","تبدیل انجام دادن به عادت حتی بدون انگیزه",0],["زبان","رسیدن به مکالمه مستقل",0],["فوتبال","پیشرفت تا عضویت در تیم",0],["سواد مالی","یادگیری مدیریت پول و سرمایه‌گذاری",0]
].map(x=>({title:x[0],text:x[1],progress:x[2]})),habits:{focus:5,discipline:1,perseverance:0},weekly:[]};
let data=JSON.parse(localStorage.getItem(KEY)||"null")||structuredClone(seed), role=sessionStorage.role||null;
function save(){localStorage.setItem(KEY,JSON.stringify(data))}
function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}
function enter(r){role=r;sessionStorage.role=r;document.getElementById("login").classList.add("hidden");document.getElementById("app").classList.remove("hidden");navigate("dashboard")}
function logout(){sessionStorage.removeItem("role");location.reload()}
function toast(t){let e=document.getElementById("toast");e.textContent=t;e.style.display="block";setTimeout(()=>e.style.display="none",1600)}
function points(){return data.tasks.filter(x=>x.done).reduce((a,x)=>a+x.points,0)}
function study(){return data.logs.filter(x=>x.date===today()&&x.type==="study").reduce((a,x)=>a+x.minutes,0)}
function pct(){return Math.round(points()/data.tasks.reduce((a,x)=>a+x.points,0)*100)}
const titles={dashboard:"داشبورد",today:"امروز",calendar:"تقویم",study:"مطالعه",exam:"آزمون",habits:"عادت‌ها",fitness:"ورزش",language:"زبان",books:"کتابخانه",finance:"سواد مالی",goals:"اهداف",parent:"پنل والد",reports:"گزارش‌ها",settings:"تنظیمات"};
function navigate(p){document.querySelectorAll("#nav button").forEach(b=>b.classList.toggle("active",b.dataset.page===p));document.getElementById("title").textContent=titles[p];render(p);document.querySelector(".sidebar").classList.remove("open")}
function metric(a,b,c){return `<div class="card"><span class="muted">${a}</span><div class="metric">${b}</div><span class="muted">${c}</span></div>`}
function render(p){({dashboard,today,calendar,study:studyPage,exam,habits,fitness,language,books,finance,goals,parent,reports,settings}[p]||dashboard)(document.getElementById("content"))}
function dashboard(c){c.innerHTML=`<div class="grid g2"><div class="card"><h3>سلام عارفه 👋</h3><div class="quote">هدف اصلی: <b>پزشکی → جراحی پلاستیک</b><br>هدف جایگزین: دندانپزشکی<br>انگیزه: ${data.profile.motivation}</div></div><div class="card"><h3>امروز</h3><div class="metric">${pct()}%</div><div class="progress"><span style="width:${pct()}%"></span></div><p class="muted">${points()} امتیاز از ${data.tasks.reduce((a,x)=>a+x.points,0)} امتیاز</p></div></div>
<div class="section"><h3>📊 شاخص‌های کلیدی</h3><button onclick="navigate('today')">ثبت عملکرد</button></div><div class="grid g4">${metric("مطالعه",study()+" دقیقه","هدف "+data.targets.study+" ساعت")}${metric("زبان",data.targets.language+" دقیقه","هدف روزانه")}${metric("کتاب",data.targets.book+" دقیقه","هدف روزانه")}${metric("گوشی","≤ "+data.targets.phone+" دقیقه","سقف روزانه")}</div>
<div class="section"><h3>🎯 مسیر رشد</h3></div><div class="grid g3">${data.goals.map(g=>`<div class="card"><h3>${esc(g.title)}</h3><p class="muted">${esc(g.text)}</p><div class="progress"><span style="width:${g.progress}%"></span></div><small>${g.progress}%</small></div>`).join("")}</div>`}
function today(c){c.innerHTML=`<div class="card"><div class="section"><h3>برنامه امروز</h3><button onclick="allDone()">انجام همه</button></div>${data.tasks.map(t=>`<label class="row"><span class="task"><input class="check" type="checkbox" ${t.done?"checked":""} onchange="task(${t.id},this.checked)">${esc(t.title)} <span class="badge">${t.cat}</span></span><b>${t.points}</b></label>`).join("")}</div><div class="section"><h3>ثبت جلسه مطالعه</h3></div><div class="card"><form class="form" onsubmit="addStudy(event)"><select id="sub">${["زیست","شیمی","فیزیک","ریاضی","فارسی","عربی","زبان"].map(x=>`<option>${x}</option>`).join("")}</select><input id="mins" type="number" min="1" placeholder="دقیقه"><button>ثبت</button></form></div>`}
function studyPage(c){let subs=["زیست","شیمی","فیزیک","ریاضی","فارسی","عربی"];c.innerHTML=`<div class="grid g3">${subs.map(s=>`<div class="card"><h3>${s}</h3><p class="muted">${s==="زیست"?"مفهوم → بازیابی → تست → مرور فاصله‌دار":"مفهوم → تمرین → تست → تحلیل"}</p><button onclick="quick('${s}',30)">ثبت ۳۰ دقیقه</button></div>`).join("")}</div><div class="section"><h3>تاریخچه</h3></div><div class="card">${data.logs.filter(x=>x.type==="study").slice().reverse().map(x=>`<div class="row"><span>${x.date} | ${esc(x.subject)}</span><b>${x.minutes} دقیقه</b></div>`).join("")||"هنوز داده‌ای ثبت نشده."}</div>`}
function exam(c){c.innerHTML=`<div class="grid g2"><div class="card"><form class="form" onsubmit="addExam(event)"><h3>ثبت آزمون</h3><input id="en" placeholder="نام آزمون"><input id="ed" type="date" value="${today()}"><input id="es" type="number" placeholder="درصد/امتیاز"><input id="ew" type="number" placeholder="تعداد غلط"><input id="eb" type="number" placeholder="تعداد نزده"><textarea id="eo" placeholder="تحلیل: مفهومی / حفظی / بی‌دقتی / زمان / سایر"></textarea><button>ذخیره</button></form></div><div class="card"><h3>قاعده تحلیل</h3><div class="quote">هر غلط باید علت داشته باشد و هر علت باید یک اقدام اصلاحی برای هفته بعد بسازد.</div></div></div><div class="card"><table class="table"><tr><th>آزمون</th><th>تاریخ</th><th>امتیاز</th><th>غلط</th><th>نزده</th></tr>${data.exams.slice().reverse().map(e=>`<tr><td>${esc(e.name)}</td><td>${e.date}</td><td>${e.score}</td><td>${e.wrong}</td><td>${e.blank}</td></tr>`).join("")}</table></div>`}
function habits(c){c.innerHTML=`<div class="grid g3">${[["focus","تمرکز"],["discipline","نظم"],["perseverance","پشتکار"]].map(([k,n])=>`<div class="card"><h3>${n}</h3><div class="metric">${data.habits[k]}/10</div><input type="range" min="0" max="10" value="${data.habits[k]}" oninput="habit('${k}',this.value)"></div>`).join("")}</div><div class="card"><h3>قواعد</h3><p>گوشی هنگام مطالعه بیرون اتاق؛ خواب هدف نهایی حدود ۲۲:۳۰ تا ۶:۳۰؛ جلسه بررسی والد هفته‌ای یک‌بار.</p></div>`}
function fitness(c){c.innerHTML=`<div class="grid g3"><div class="card"><h3>⚽ فوتبال</h3><p>یکشنبه، چهارشنبه، پنجشنبه</p><span class="badge">هدف: عضویت در تیم</span></div><div class="card"><h3>🏋️ باشگاه</h3><p>۳ روز در هفته</p></div><div class="card"><h3>📅 ثبت تمرین</h3><form class="form" onsubmit="sport(event)"><input id="sn" placeholder="نوع تمرین"><input id="sm" type="number" placeholder="دقیقه"><button>ثبت</button></form></div></div><div class="card"><h3>تمرین‌های ثبت‌شده</h3>${data.sports.slice().reverse().map(s=>`<div class="row"><span>${s.date} | ${esc(s.name)}</span><b>${s.minutes} دقیقه</b></div>`).join("")||"هنوز تمرینی ثبت نشده."}</div>`}
function language(c){c.innerHTML=`<div class="grid g3"><div class="card"><h3>🇬🇧 English File 1</h3><p>۳ روز در هفته</p></div><div class="card"><h3>🎧 Listening</h3><button onclick="quick('Listening',10)">ثبت ۱۰ دقیقه</button></div><div class="card"><h3>🗣️ Speaking</h3><button onclick="quick('Speaking',5)">ثبت ۵ دقیقه</button></div></div><div class="card"><h3>مسیر</h3><p>A1 → A2 → B1 → B2 → مکالمه مستقل</p></div>`}
function books(c){c.innerHTML=`<div class="card"><form class="form" onsubmit="book(event)"><h3>افزودن کتاب</h3><input id="bt" placeholder="نام کتاب"><select id="bc"><option>رشد فردی</option><option>داستان و رمان</option><option>سواد مالی</option><option>علم و پزشکی</option><option>مهارت ارتباطی</option></select><button>افزودن</button></form></div><div class="section"><h3>کتابخانه</h3></div><div class="grid g3">${data.books.map((b,i)=>`<div class="card"><h3>${esc(b.title)}</h3><span class="badge">${esc(b.cat)}</span><p class="muted">هدف: ۲۰ دقیقه در روز</p><button class="danger" onclick="delBook(${i})">حذف</button></div>`).join("")||'<div class="card">هنوز کتابی اضافه نشده.</div>'}</div>`}
function finance(c){c.innerHTML=`<div class="grid g3"><div class="card"><h3>دهم</h3><p>بودجه، هزینه، درآمد، پس‌انداز.</p></div><div class="card"><h3>یازدهم</h3><p>تورم، سود مرکب، سرمایه‌گذاری، کسب‌وکار.</p></div><div class="card"><h3>دوازدهم</h3><p>تصمیم‌گیری مالی و مدیریت پول.</p></div></div><div class="card"><h3>تمرین ماهانه</h3><textarea id="fn" class="form" placeholder="این ماه چه چیزی درباره پول یاد گرفتم؟"></textarea><button onclick="toast('ذخیره شد')">ذخیره</button></div>`}
function goals(c){c.innerHTML=`<div class="grid g2">${data.goals.map((g,i)=>`<div class="card"><h3>${esc(g.title)}</h3><p>${esc(g.text)}</p><input type="range" min="0" max="100" value="${g.progress}" oninput="goal(${i},this.value)"><b>${g.progress}%</b></div>`).join("")}</div>`}
function parent(c){c.innerHTML=`<div class="grid g4">${metric("انجام برنامه",pct()+"%","امروز")}${metric("مطالعه",study()+" دقیقه","امروز")}${metric("امتیاز",points(),"امروز")}${metric("وضعیت",pct()>=70?"🟢":"🟡","نیازمند پیگیری")}</div><div class="grid g2 section"><div class="card"><h3>گزارش والد</h3><p>اگر چند روز افت پشت‌سرهم دیده شد، به‌جای فشار بیشتر علت را بررسی و حجم برنامه را اصلاح کنید.</p><button onclick="weeklyReport()">گزارش هفتگی</button></div><div class="card"><h3>هدف نظارت</h3><p>عارفه خودش ثبت می‌کند؛ شما روند را می‌بینید و فقط جلسه هفتگی را مدیریت می‌کنید.</p></div></div>`}
function reports(c){let days=[...Array(7)].map((_,i)=>{let d=new Date();d.setDate(d.getDate()-6+i);let s=d.toISOString().slice(0,10);return {d:s,m:data.logs.filter(x=>x.date===s&&x.type==="study").reduce((a,x)=>a+x.minutes,0)}});let max=Math.max(1,...days.map(x=>x.m));c.innerHTML=`<div class="card"><h3>📈 مطالعه ۷ روز اخیر</h3><div class="chart">${days.map(x=>`<div class="bar" style="height:${Math.max(4,x.m/max*130)}px"><b>${x.m}</b><span>${x.d.slice(5)}</span></div>`).join("")}</div></div><div class="section"><h3>خلاصه مدیریتی</h3></div><div class="card"><p>امروز ${study()} دقیقه مطالعه ثبت شده و ${points()} امتیاز گرفته شده است.</p><p>تمرکز ${data.habits.focus}/10 | نظم ${data.habits.discipline}/10 | پشتکار ${data.habits.perseverance}/10</p></div>`}
function calendar(c){c.innerHTML=`<div class="card"><h3>تقویم هفتگی ثابت</h3><p><span class="pill">مدرسه: حدود ۷ تا ۱۴</span><span class="pill">فوتبال: یکشنبه</span><span class="pill">فوتبال: چهارشنبه</span><span class="pill">فوتبال: پنجشنبه</span><span class="pill">باشگاه: ۳ روز</span><span class="pill">زبان: ۳ روز</span></p><div class="notice">برنامه دقیق ساعت‌به‌ساعت باید بعد از قطعی شدن ساعت کلاس‌ها تنظیم شود؛ داشبورد برای تغییر سریع آن آماده است.</div></div>`}
function settings(c){c.innerHTML=`<div class="grid g2"><div class="card"><form class="form" onsubmit="settingsSave(event)"><h3>اهداف روزانه</h3><label>مطالعه (ساعت)<input id="ts" type="number" step=".5" value="${data.targets.study}"></label><label>زبان (دقیقه)<input id="tl" type="number" value="${data.targets.language}"></label><label>کتاب (دقیقه)<input id="tb" type="number" value="${data.targets.book}"></label><label>سقف گوشی (دقیقه)<input id="tp" type="number" value="${data.targets.phone}"></label><button>ذخیره</button></form></div><div class="card"><h3>پشتیبان و بازیابی</h3><button onclick="backup()">دانلود پشتیبان</button><input id="restore" type="file" accept=".json" onchange="restore(event)"><p class="muted">نسخه آفلاین روی همین دستگاه کار می‌کند.</p><button class="danger" onclick="wipe()">حذف همه اطلاعات</button></div></div>`}
function task(i,v){data.tasks[i].done=v;save();render("today")} function allDone(){data.tasks.forEach(x=>x.done=true);save();render("today");toast("برنامه امروز ثبت شد")}
function addStudy(e){e.preventDefault();data.logs.push({date:today(),type:"study",subject:sub.value,minutes:+mins.value});save();render("today");toast("ثبت شد")}
function quick(subject,minutes){data.logs.push({date:today(),type:"study",subject,minutes});save();render("study");toast("ثبت شد")}
function addExam(e){e.preventDefault();data.exams.push({name:en.value,date:ed.value,score:+es.value||0,wrong:+ew.value||0,blank:+eb.value||0,note:eo.value});save();render("exam");toast("آزمون ثبت شد")}
function habit(k,v){data.habits[k]=+v;save()} function goal(i,v){data.goals[i].progress=+v;save()}
function sport(e){e.preventDefault();data.sports.push({date:today(),name:sn.value,minutes:+sm.value||0});save();render("fitness");toast("تمرین ثبت شد")}
function book(e){e.preventDefault();data.books.push({title:bt.value,cat:bc.value});save();render("books");toast("کتاب اضافه شد")}
function delBook(i){data.books.splice(i,1);save();render("books")}
function settingsSave(e){e.preventDefault();data.targets.study=+ts.value;data.targets.language=+tl.value;data.targets.book=+tb.value;data.targets.phone=+tp.value;save();toast("ذخیره شد");render("settings")}
function backup(){let a=document.createElement("a"),b=new Blob([JSON.stringify(data,null,2)],{type:"application/json"});a.href=URL.createObjectURL(b);a.download="arefeh-backup-"+today()+".json";a.click()}
function restore(e){let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>{try{data=JSON.parse(r.result);save();toast("بازیابی شد");navigate("dashboard")}catch{toast("فایل معتبر نیست")}};r.readAsText(f)}
function wipe(){if(confirm("همه اطلاعات پاک شود؟")){localStorage.removeItem(KEY);location.reload()}}
function weeklyReport(){let text=`گزارش هفتگی عارفه\nامروز: ${today()}\nمطالعه امروز: ${study()} دقیقه\nانجام برنامه: ${pct()}%\nامتیاز: ${points()}\nتمرکز: ${data.habits.focus}/10\nنظم: ${data.habits.discipline}/10\nپشتکار: ${data.habits.perseverance}/10\nآزمون‌ها: ${data.exams.length}`;navigator.clipboard?.writeText(text);toast("گزارش کپی شد")}
function toggleSide(){document.querySelector(".sidebar").classList.toggle("open")}
document.querySelectorAll("#nav button").forEach(b=>b.onclick=()=>navigate(b.dataset.page));document.getElementById("date").textContent=new Intl.DateTimeFormat("fa-IR",{dateStyle:"full"}).format(new Date());
if(role)enter(role);
